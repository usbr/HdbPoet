
The files in this directory OpenSourceDependecy are alternatives to the files
in the LicensdedDepedency.  Only select Compile (in Properties) for all files one directory
and None for all files in the other.